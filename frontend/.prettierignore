# Regenerated by the TanStack Router vite plugin on every dev/build run in
# its own style - not hand-written, not formatted. Committed (see the
# scaffold commit's message for why), but excluded from format/format:check
# so a routine regeneration doesn't fail the pre-commit hook or CI.
src/routeTree.gen.ts
